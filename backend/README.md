# GradeVision Backend

## Instalación

Con el entorno virtual de esta carpeta activado:

pip install -r requirements.txt
pip install -e ..

La segunda línea instala `gradevision_core` desde la carpeta del proyecto (un nivel arriba), en modo editable.